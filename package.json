{
  "name": "tricket",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "migrate-dev": "dotenv -e .env.development -- npx prisma migrate dev",
    "migrate-test": "dotenv -e .env.test -- npx prisma migrate dev",
    "migrate-prod": "dotenv -e .env.production -- npx prisma migrate dev",
    "push-dev": "dotenv -e .env.development -- npx prisma db push --force-reset",
    "push-test": "dotenv -e .env.test -- npx prisma db push --force-reset",
    "push-prod": "dotenv -e .env.production -- npx prisma db push --force-reset",
    "migrate-reset-dev": "dotenv -e .env.development -- npx prisma migrate reset dev",
    "migrate-reset-test": "dotenv -e .env.test -- npx prisma migrate reset dev",
    "migrate-reset-prod": "dotenv -e .env.production -- npx prisma migrate reset dev",
    "generate-dev": "dotenv -e .env.development -- npx prisma generate dev",
    "generate-test": "dotenv -e .env.test -- npx prisma generate dev",
    "generate-prod": "dotenv -e .env.production -- npx prisma generate dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.5",
    "express": "^4.18.2",
    "prisma": "^5.8.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@prisma/client": "^5.8.1"
  },
  "prisma": {
    "seed": "./prisma/seed/seed.sh"
  }
}
